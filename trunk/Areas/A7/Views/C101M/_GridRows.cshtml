@using EECOnline.Areas.A7.Models;
@model C101MFormModel

@if (Model.Grid != null)
{
    for (int i = 0; i < Model.Grid.Count; i++)
    {
        @Html.HiddenFor(m =>m.Grid[i].enews_id);
        var item = Model.Grid[i];
        <tr>
            <td>
                <div align="center"weidth="10%">@item.row_id</div>
            </td>
            <td>
                <div align="center">@item.code_name</div>
            </td>
            <td>
                <div align="center">@item.subject</div>
            </td>
            <td>
                <div align="center">@item.showdates</div>
            </td>
            <td>
                <div align="center">@item.showdatee</div>
            </td>
            <td>
                <div align="center">@item.showyn</div>
            </td>
            <td>
                <div align="center">@item.totop</div>
            </td>
            <td align="center">
                <button type="button" class="btn btn-blue" onclick="doModify('@item.enews_id')">編輯</button>
            </td>
        </tr>
    }
}