@using EECOnline.Areas.A3.Models;
@using EECOnline.Services;
@model C102MFormModel

@if (Model.Grid != null)
{
    for (int i = 0; i < Model.Grid.Count; i++)
    {
        var item = Model.Grid[i];
        <tr>
            <td align="center">@item.AccountingYM_Text</td>
            <td align="center">@item.hospital_name</td>
            <td align="right" style="padding-right: 9rem;">@item.price_Text</td>
        </tr>
    }
}