@using ES.Models
@{
    SessionModel sm = SessionModel.Get();

    bool flag_NG = false;
    if (!flag_NG && sm == null) { flag_NG = true; }
    if (!flag_NG && sm.UserInfo == null) { flag_NG = true; }
    if (!flag_NG && sm.UserInfo.Member == null) { flag_NG = true; }

}
<div class="container-fluid">
    <div class="col-sm-12 header-link text-right">
        <a href="@(Url.Action("FAQ", "Login"))" target="_blank"><i class="fa fa-heart"></i>問與答</a>
        <a href="@(Url.Action("OpenMail", "Login"))" target="_blank"><i class="fa fa-book"></i>服務信箱</a>
        <a href="@(Url.Action("Trace", "Login"))" target="_blank"><i class="fa fa-search"></i>網站導覽</a>
        @if (flag_NG)
        {
            <a href="@(Url.Action("Index", "Login"))"><i class="fa fa-user"></i>會員登入</a>
        }
        else
        {
            <a href="@(Url.Action("Logout", "Login"))"><i class="fa fa-user"></i>@sm.UserInfo.Member.NAME ，您好  登出</a>
        }

        <a href="https://www.mohw.gov.tw/mp-1.html" target="_blank"><i class="fa fa-home"></i>造訪衛福部</a>
        @*<a href="qna.html"><i class="fa fa-info-circle"></i>常見問題</a>
            <a href="#"><i class="fa fa-envelope"></i>聯絡我們</a>*@
    </div>
    <!--logo -->
    <div class="col-sm-12">
        <div class="navbar-logo"><a href="@(Url.Action("Index", "Login"))"><img src="~/images/logo.png" class="img-responsive" alt="衛生福利部｜線上申辦服務系統" title="衛生福利部｜線上申辦服務系統"></a></div>

    </div>

</div>

<div class="clearfix"></div>

<nav class="navbar-bg">


    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                <li><a id="lihead_Message" href="@(Url.Action("Index", "Message"))">最新消息</a></li>
                <li><a id="lihead_ServiceLst" href="@(Url.Action("Index", "ServiceLst"))">申辦服務</a></li>
                <li><a id="lihead_Service" href="@(Url.Action("Index", "Service"))">表件下載</a></li>
                <li><a id="lihead_History" href="@(Url.Action("Index", "History"))">案件進度查詢</a></li>
                <li><a id="lihead_QuestNonWeb" href="@(Url.Action("Index", "QuestNonWeb"))">滿意度調查</a></li>
                <li><a id="lihead_Site" href="@(Url.Action("Index", "Site"))">友善連結</a></li>
                <li><a id="lihead_Login" href="@(Url.Action("Index", "Login"))">會員專區</a></li>
            </ul>
        </div>
    </div>
</nav>
